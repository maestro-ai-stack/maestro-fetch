## What changed

<!-- One-paragraph summary. Link any related issue with "Closes #N". -->

## How to test

```bash
# commands to verify this PR works
uv run pytest tests/
mfetch <example url>
```

## Checklist

- [ ] Tests pass (`uv run pytest`)
- [ ] Type-check passes (`uv run pyright src/`)
- [ ] `CHANGELOG.md` updated under `## Unreleased`
- [ ] No files over 800 lines
